# 깨비즈 웹 (ggaebiz-web)

## Git 커밋 규칙

- 커밋 메시지는 **한국어 문장형**으로 작성한다. (예: `히어로 섹션 추가함`, `다운로드 CTA 스타일 수정함`)
- **기능·주제 단위**로 나누어 커밋한다. 한 커밋에 서로 무관한 변경을 섞지 않는다.
- 빌드·테스트·린트 등 **실행이 필요한 작업**을 하기 전에는 먼저 무엇을 실행할지 알려준 뒤 진행한다.
- 커밋은 항상 (1) 기능 단위로 분리해 메시지 목록을 먼저 보여주고, (2) 사용자 승인 후에만 실제로 실행한다. **자동 커밋 금지.**
